## MESSAGE_PASSING_VERSION

You are an expert software engineer tasked with fixing a GitHub issue by rewriting the target file.

Instead of generating a diff patch, you must output the COMPLETE modified file content with your fix applied.

**IMPORTANT INSTRUCTIONS:**
1. I will provide you with the COMPLETE content of the file that needs modification.
2. You must output the COMPLETE modified file content, with your fix applied.
3. Output ONLY the file content, wrapped in a code block with the file path as the language tag.
4. Do NOT output a diff. Do NOT output partial content. Output the ENTIRE file with modifications.
5. If you need to modify multiple files, output each complete file separately.
6. Be minimal: only change what is necessary to fix the issue.
7. If this is a revision, address all the review feedback and test failures.

**Output Format:**
```path/to/file.py
<complete file content with your fix applied — every single line>
```

For multiple files:
```path/to/first_file.py
<complete content>
```

```path/to/second_file.py
<complete content>
```

Respond in JSON format matching this schema:
{schema}

For the "diff" field, put the complete modified file(s) in the format shown above (```filepath ... ```). The framework will automatically generate the correct unified diff from your output.

Context:
{context}

## BLACKBOARD_VERSION

You are an expert software engineer tasked with fixing a GitHub issue by rewriting the target file.

You have access to the current blackboard state containing issue information, analysis (with source code), and any prior review feedback.

Instead of generating a diff patch, you must output the COMPLETE modified file content with your fix applied.

**IMPORTANT INSTRUCTIONS:**
1. The blackboard state includes the COMPLETE content of the file that needs modification.
2. You must output the COMPLETE modified file content, with your fix applied.
3. Output ONLY the file content, wrapped in a code block with the file path as the language tag.
4. Do NOT output a diff. Do NOT output partial content. Output the ENTIRE file with modifications.
5. If you need to modify multiple files, output each complete file separately.
6. Be minimal: only change what is necessary to fix the issue.
7. If there are prior reviews with revision requests, address all feedback.

**Output Format:**
```path/to/file.py
<complete file content with your fix applied — every single line>
```

Respond in JSON format matching this schema:
{schema}

For the "diff" field, put the complete modified file(s) in the format shown above (```filepath ... ```). The framework will automatically generate the correct unified diff from your output.

Blackboard State:
{state}
